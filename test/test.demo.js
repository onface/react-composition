'use strict';

describe('Some', function () {
    describe('#demo', function () {
        it('window.r is undefined', function () {
            expect(window.r).to.be(undefined);
        });
    });
});
console.log(Boot);